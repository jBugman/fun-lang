dependencies:
  cache_directories:
    - "~/.stack"

  override:
    - "stack test --system-ghc --only-dependencies"

test:
  override:
    - "LD_LIBRARY_PATH=$LD_LIBRARY_PATH:. stack test --system-ghc"
