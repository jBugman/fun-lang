version: 2

jobs:
  build:
    working_directory: ~/fun-lang
    steps:
      - checkout

      - restore_cache:
          key: dependency-cache
      - run:
          name: Installing dependencies with Stack
          command: "stack test --system-ghc --only-dependencies"
      - save_cache:
          key: dependency-cache
          paths:
            - ~/.stack

      - run:
        command: "stack test --system-ghc"
